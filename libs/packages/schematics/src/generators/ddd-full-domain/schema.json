{
  "$schema": "http://json-schema.org/schema",
  "$id": "Domain",
  "title": "",
  "type": "object",
  "properties": {
    "appName": {
      "type": "string",
      "description": "What is the app name?",
      "x-prompt": "What is the app name?"
    },
    "domain": {
      "type": "string",
      "description": "What is te domain?",
      "x-prompt": "What is te domain?"
    }
  },
  "required": [
    "appName",
    "domain"
  ]
}
