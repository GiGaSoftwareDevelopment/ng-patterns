import { firebaseConfig } from '@secrets';
import { BookingEnvironmentModel } from '@<%= domain %>/domain';

export const environment: <%= className %>EnvironmentModel = {
  production: false,
  development: true,
  uat: false,
  qa: false,
  firebaseConfig
};

