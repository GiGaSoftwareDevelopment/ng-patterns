import { firebaseConfig } from '@secrets';
import { BookingEnvironmentModel } from '@<%= appName %>/<%= domain %>/domain';

export const environment: <%= className %>EnvironmentModel = {
  production: false,
  development: false,
  uat: false,
  qa: true,
  firebaseConfig
};

