{
  "$schema": "http://json-schema.org/schema",
  "$id": "Environment",
  "title": "",
  "type": "object",
  "properties": {
    "appName": {
      "type": "string",
      "description": "What is the app project directory name?",
      "x-prompt": "What is the app project directory name?"
    },
    "domain": {
      "type": "string",
      "description": "What is the domain directory name?",
      "x-prompt": "What is the domain directory name?"
    }
  },
  "required": [
    "appName",
    "domain"
  ]
}
