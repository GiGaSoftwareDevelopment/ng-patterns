{
  "$schema": "http://json-schema.org/schema",
  "$id": "Environment",
  "title": "",
  "type": "object",
  "properties": {
    "appName": {
      "type": "string",
      "description": "What is the app project directory name?",
      "x-prompt": "What is the app project directory name?"
    },
    "domain": {
      "type": "string",
      "description": "What is the domain directory name?",
      "x-prompt": "What is the domain directory name?"
    },
    "workspaceName": {
      "type": "string",
      "description": "What is the workspace ( or respository ) name?",
      "x-prompt": "What is the workspace ( or respository ) name?"
    }
  },
  "required": [
    "appName",
    "domain"
  ]
}
