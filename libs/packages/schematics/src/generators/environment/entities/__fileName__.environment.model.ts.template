import { InjectionToken } from '@angular/core';
import { FirebaseConfig } from '@ngpat/firebase';

export interface BookingEnvironmentModel {
  production: boolean;
  development: boolean;
  uat: boolean;
  dev: boolean;
  firebaseConfig: FirebaseConfig
}

export const BOOKING_ENVIRONMENT: InjectionToken<BookingEnvironmentModel> = new InjectionToken<BookingEnvironmentModel>('BOOKING_ENVIRONMENT')
