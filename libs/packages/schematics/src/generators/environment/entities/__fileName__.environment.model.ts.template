import { InjectionToken } from '@angular/core';
import { FirebaseConfig } from '@ngpat/firebase';

export interface <%= className %>EnvironmentModel {
  production: boolean;
  development: boolean;
  uat: boolean;
  qa: boolean;
  firebaseConfig: FirebaseConfig
}

export const <%= APP_NAME %>_ENVIRONMENT: InjectionToken<<%= className %>EnvironmentModel> = new InjectionToken<<%= className %>EnvironmentModel>('<%= APP_NAME %>_ENVIRONMENT')
